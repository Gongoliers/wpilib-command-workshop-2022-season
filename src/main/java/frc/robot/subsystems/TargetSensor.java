package frc.robot.subsystems;

public class TargetSensor {
    public TargetSensor(){}

    /**
     * Determines the angle to the target - negative means the robot needs to rotate to the left, positive means it needs to rotate to the right
     */
    public double getAngleToTarget(){
        return 0.0;
    }
}